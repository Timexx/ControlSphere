# -- ControlSphere Docker Compose (OPTIONAL) --------------------------------
# This file is NOT required. All values below are auto-generated on first
# startup and persisted in the secrets_data Docker volume.
#
# Only create this file if you want to OVERRIDE the defaults.
# Copy to .env and uncomment the values you want to change.

# PostgreSQL password (internal only — not exposed to the network)
# POSTGRES_PASSWORD=my-custom-password

# JWT signing secret
# JWT_SECRET=my-custom-jwt-secret

# Session HMAC key (32-byte hex string)
# SESSION_TOKEN_SECRET=my-custom-session-secret

# Host port (default: 3000)
# PORT=3000
