# ── Docker Compose environment ────────────────────────────────────────────
# Copy this file to .env and fill in the values, then run:
#   docker compose up -d

# PostgreSQL password (used for both the postgres service and DATABASE_URL)
# Use a strong random password in production
POSTGRES_PASSWORD=change-me-strong-password

# JWT signing secret — AUTO-GENERATED on first startup if left empty.
# The generated value is stored in the `secrets_data` Docker volume and reused
# on every subsequent restart. Override here only if you need a fixed value.
JWT_SECRET=

# Session HMAC key (32-byte hex string) — AUTO-GENERATED on first startup if left empty.
SESSION_TOKEN_SECRET=

# Host port to expose the server on (default: 3000)
PORT=3000
